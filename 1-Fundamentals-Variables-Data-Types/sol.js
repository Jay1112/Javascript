/* Assignment 1 */

// variables
let price = 1000;
let discount = 25 ; 

// apply the discount on an item
price = price - (price*(discount/100))

// new price
console.log(price);

/* Assignment 2 */

// variables 
let radius = 50 ; 
const PI = Math.PI ; 

// calculate the area
let area = PI * (radius**2) ;
area = Math.round(area);

console.log(area);

